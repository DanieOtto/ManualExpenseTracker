<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:microCharts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             x:Class="ManualExpenseTracker.Pages.HomePage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0"
                   HorizontalOptions="Center"
                   Margin="20"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Text="Welcome to Manual Expense Tracker"/>
            
            <microCharts:ChartView
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Chart="{Binding overviewChart}" />
            
            <Button Text="Add Expense"
                    Margin="20"
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    FontSize="24"
                    FontAttributes="Bold"
                    TextColor="White"
                    BackgroundColor="#2196F3" />
        </Grid>
    </ContentPage.Content>
    

</ContentPage>